//WAP to find rectaangle

#include<stdio.h>
int main()
{
    int length, width;

    printf("enter length of rectangle: ");
    scanf("%d",&length);
    printf("enterr width of rectangle:");
    scanf("%d",&width);

    printf("aarea of rectanglee is %d",length*width);
    return 0;
}